今天遇到了一个问题是一个无序的列表，经过set()去重之后被自动排序了
这种情况应该要非常注意，要保持原来顺序的话
                a = [1,1,1,4,4,4,2,2,2,]
                b = list(set(a))
                b.sort(key = a.index)
                print(b)
使用原来的列表索引再进行排序即可